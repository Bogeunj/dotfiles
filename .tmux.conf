# Prefix를 Ctrl + a로 설정
set -g prefix C-a
unbind C-b
bind C-a send-prefix

# 에피의 바이브: 창 번호를 1번부터 시작! (매우 중요)
set -g base-index 1
setw -g pane-base-index 1

# 창 번호가 중간에 비면 자동으로 채우기
set -g renumber-windows on
